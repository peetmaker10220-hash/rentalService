spring.application.name=ngage-modelservices-dbservices
jwt.secret= Vproject
spring.profiles.active=local

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# datasource.local.driverClassName = org.postgresql.Driver
# datasource.local.url = jdbc:postgresql://
# datasource.local.username = 
# datasource.local.password = 

# datasource.driverClassName = 
# datasource.url = jdbc:postgresql://
# datasource.username = 
# datasource.password = 

